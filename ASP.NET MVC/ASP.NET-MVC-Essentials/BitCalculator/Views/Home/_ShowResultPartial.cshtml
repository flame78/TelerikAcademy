@model List<BitCalculator.Models.ResultData>

<table class="table table-striped table-condensed">
    @foreach (var result in Model)
    {
        <tr>
            <td>
                @result.Type
            </td>
            <td>
                @string.Format("{0}",result.Value)
            </td>
        </tr>
    }
</table>