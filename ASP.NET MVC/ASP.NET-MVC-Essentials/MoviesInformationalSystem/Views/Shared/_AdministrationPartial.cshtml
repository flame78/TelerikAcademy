@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown">
            Administration
            <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
            <li>
                @Html.ActionLink("Actors", "Index", "Actors", new { Area = "Administration" }, null)
            </li>
            <li>
                @Html.ActionLink("Movies", "Index", "Movies", new { Area = "Administration" }, null)
            </li>
            <li>
                @Html.ActionLink("Studios", "Index", "Studios", new { Area = "Administration" }, null)
            </li>
            <li class="divider"></li>
            <li>
                @Html.ActionLink("Users", "Index", "Users", new { Area = "Administration"}, null)
            </li>
        </ul>
    </li>
}
